# SysTick
```c
// 参考: 000.STM32/000.STM32F103C8T6/000.STM32F103C8T6_1/013.STM32_Breathe_LED/src/main.c 
    {
        // 配置时钟来源
        SysTick_CLKSourceConfig(SysTick_CLKSource_HCLK);
        // 设置重载值
        SysTick_Config(SystemCoreClock / 10000);
        NVIC_SetPriority(SysTick_IRQn, -1);
    }
```